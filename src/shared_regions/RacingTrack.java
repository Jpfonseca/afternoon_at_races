package shared_regions;
import entities.*;
public class RacingTrack{
    void proceedToTheStartLine(HorseJockey horse){
        // sends horse_jockey to the start_Line
        // bloqueia em  waitForA
    }

    void makeAmove(HorseJockey horse){
        // makeAMove
        //bloqueia a andar e acorda o seguinte se o mesmo não tiver passado linha de corrida
    }

    boolean hasFinishLineBeenCrossed(HorseJockey horse){
        // Verify line crossed
        return false;
    }

}